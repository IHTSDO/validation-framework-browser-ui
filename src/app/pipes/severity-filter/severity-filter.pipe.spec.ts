import { SeverityFilterPipe } from './severity-filter.pipe';

describe('SeverityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SeverityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
