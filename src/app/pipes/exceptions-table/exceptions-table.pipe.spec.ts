import { ExceptionsTablePipe } from './exceptions-table.pipe';

describe('ExceptionsTablePipe', () => {
  it('create an instance', () => {
    const pipe = new ExceptionsTablePipe();
    expect(pipe).toBeTruthy();
  });
});
